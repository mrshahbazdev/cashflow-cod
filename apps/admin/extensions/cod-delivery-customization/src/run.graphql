query RunInput {
  cart {
    deliveryGroups {
      deliveryAddress {
        city
      }
    }
  }
  deliveryOptions {
    id
    title
  }
  shop {
    metafield(namespace: "cashflow", key: "function-config") {
      value
    }
  }
}
