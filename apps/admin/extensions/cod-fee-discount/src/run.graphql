query RunInput {
  cart {
    cost {
      totalAmount {
        amount
        currencyCode
      }
    }
  }
  shop {
    metafield(namespace: "cashflow", key: "function-config") {
      value
    }
  }
}
