query RunInput {
  cart {
    cost {
      totalAmount {
        amount
      }
    }
  }
  paymentMethods {
    id
    name
  }
  shop {
    metafield(namespace: "cashflow", key: "function-config") {
      value
    }
  }
}
